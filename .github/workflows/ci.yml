name: CI

on:
  push:
  pull_request:

jobs:
  python-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest

      - name: Run repository test suite
        run: pytest tests/ -q

      - name: Run chunking regression suite
        run: HF_HUB_OFFLINE=1 pytest multilingual/tests/test_chunking_strategies.py -q

  frontend-qa:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install frontend dependencies
        run: npm ci

      - name: Build local preview bundle
        run: npm run build:web

      - name: Verify checked-in bundle is current
        run: git diff --exit-code -- local_preview/web/assets

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run answer-mode regression
        run: npx playwright test e2e/qa-answer-mode.spec.ts
